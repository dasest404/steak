#
# Source files (blade, markdown, etc.)
#
source:
  directory: docs
  git:
    url: 'https://github.com/parsnick/steak.git'
    branch: gh-pages-src

build:
  directory: build

deploy:
  git:
    url: 'https://github.com/parsnick/steak.git'
    branch: gh-pages

serve:
  subdirectory: steak
